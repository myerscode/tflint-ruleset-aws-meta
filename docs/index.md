---
layout: default
title: tflint-ruleset-aws-meta
---

# tflint-ruleset-aws-meta

This repository provides custom rules for tflint focused on AWS metadata (regions, partitions, service principals, etc.).

- See the full rule reference: [Rules](/rules.md)

## Quick links

- Repository: https://github.com/myerscode/tflint-ruleset-aws-meta
- Docs source: `/docs` (this folder)

> To publish these docs as a GitHub Pages site, open the repository Settings → Pages and set the "Source" to the `docs` folder on the `main` branch (or `master` if that's your default branch). Jekyll will render the markdown files in this folder as pages.
